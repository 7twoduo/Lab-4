# Lab-4
Each Step Wait for terraform to run and complete
Wait for completion
Wait for completion
Nah Ill just force you to wai


1st run terraform apply -auto-approve in Japan

2nd  go to Iowa, create a directory called hidden_domain and create a public and private key and store in two files called fullchain.pem for public key and privkey.pem for private key. 

3rd Go to line 308 in 1-main.tf and turn it from false to true. Then run terraform apply -auto-approve.

4rd Go to Japan and go to line 710 in main.tf and turn it from false to true. Run terraform apply -auto-approve.

5th Go to Japan and go to line 742 in main.tf and turn it from false to true. Run terraform apply -auto-approve.

6th Go to Iowa and go to line 324 in 1-main.tf and turn it from false to true. Run terraform apply -auto-approve.

7th Go to Japan and go to line 833 in  main.tf and turn it from false to true. Run terraform apply -auto-approve

8th Go to Japan and run terraform apply -auto-approve again in japan to add the route to the route table. 

8th Go to Japan and run terraform apply -auto-approve again

9th There we go, you are done and so vpc(AWS-tgw-ha-vpn-bgp-vpnc(GCP)



Trying to use terraform state backup is a possible good option but I think it best to just point it to state so as to ensure never to not have drift.
Don't run on tfstate backup, just warning you. 

